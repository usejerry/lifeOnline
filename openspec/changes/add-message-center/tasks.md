## 1. 数据结构与发布准备（lifeOnline）

- [ ] 1.1 新增消息实体、唯一去重键和查询索引，接取记录增加可空 expiresAt/expiredAt 和 expired 状态；通过实体构建与结构检查确认字段、类型和索引符合 design.md。
- [ ] 1.2 提供版本化向前 SQL、结构核验及保留数据的回滚说明，更新部署文档；在隔离 MySQL 中迁移含旧记录的数据集，确认原记录期限仍为 null 且数据未改写。
- [ ] 1.3 新增控制“新接取写期限”的配置并接入校验，保留已有期限记录执法独立运行；测试开关前后接取分别无期限／有期限，关闭后已有期限仍生效。

## 2. 支线期限与状态转换（lifeOnline）

- [ ] 2.1 在接取事务内以接取时 durationMinutes 保存固定截止时间，用户行锁串行化名额检查；测试20分钟任务的24小时20分钟期限及修改任务后快照不变，MySQL 并发接取仅一条成功。
- [ ] 2.2 实现到期状态协调并接入 active、列表、详情和接取入口；测试截止等号、后台未运行时释放名额、已完成／已放弃不覆盖、旧 null 期限记录保持原样。
- [ ] 2.3 为完成／放弃增加最终原子期限条件和409 QUEST_EXPIRED 响应；测试外部位置／天气校验期间跨截止时间、完成与过期竞争、错误响应不回滚已过期状态，并确认失败上传文件被清理。
- [ ] 2.4 扩展接取记录响应和查询 DTO，新增本人记录详情接口；验证 expired 筛选、越权404、默认 completed 查询兼容及过期记录不计入完成统计。

## 3. 消息存储与接口（lifeOnline）

- [ ] 3.1 创建消息模块并注册鉴权、分页列表、未读数量和详情接口，批量计算关联业务状态和受控 action；接口测试覆盖分页上限、稳定排序、空列表、未登录和跨用户隔离。
- [ ] 3.2 实现单条及全部已读，幂等保留首次 readAt，阅读与业务状态解耦；测试重复操作、全部已读后的新消息仍未读以及业务完成不自动已读。
- [ ] 3.3 实现消息业务状态映射，覆盖已签到、跨日已过期、任务完成／放弃／过期和关联不可用；验证无效操作消失，旧消息始终关联原接取记录。

## 4. 签到与临期提醒（lifeOnline）

- [ ] 4.1 复用统一北京时间日期计算，实现 POST /v1/messages/sync 的今日签到检查及重复键去重；测试未签到生成、已签到不生成、跨日、长期离线不补发、多设备并发最多一条及已读不重置。
- [ ] 4.2 接入轻量调度依赖，实现30秒批次扫描、启动补偿及请求端本人补偿，按先过期后临期顺序处理；可控时钟测试窗口开始、截止等号、恢复时仍临期补发、恢复时已过期不补发。
- [ ] 4.3 临期创建时锁定并复查接取记录，加入批量大小、游标、错误记录与有限重试；MySQL 集成测试验证多实例去重、与完成／放弃竞争以及批次失败后恢复，无无限重复或漏扫。
- [ ] 4.4 保留签到与成长积分事务行为，验证提醒创建失败不会改变已成功签到；运行现有 sign-in 与 growth 相关测试并补充签到与提醒并发后的状态检查。

## 5. 消息中心页面与阅读交互（lifeOnline-h5）

- [ ] 5.1 新增 messages API、类型和 Pinia 状态，接入单一全局刷新管理器；测试登录恢复、可见性切换、跨日、轮询清理和切换账号时丢弃旧请求结果，类型检查通过。
- [ ] 5.2 新增受保护 /messages 页面及底部消息入口、未读角标、分页、空态和失败重试；浏览器确认不同移动宽度可用、99+显示与可访问标签正确，未登录进入会跳转登录。
- [ ] 5.3 实现点击已读再按最新 action 跳转、全部已读及请求失败反馈；交互验证打开列表不已读、单条操作不影响其他消息、双设备刷新一致及失败时不伪报成功。
- [ ] 5.4 在个人页面为现有签到组件增加可定位入口，并在签到成功后刷新消息；验证提醒可到达签到位置，签到后消息显示已签到且未读状态保持独立。

## 6. 支线到期与历史展示（lifeOnline-h5）

- [ ] 6.1 扩展 quest-record API、journey 状态和今日页面，显示新任务截止时间并处理 QUEST_EXPIRED；验证到期后操作禁用及服务器409提示正确、旧任务不显示虚构期限。
- [ ] 6.2 新增 /records/:id 受保护详情，记录页面增加过期筛选而保留默认完成纪念卡；验证已过期历史可查看、无完成按钮且不增加完成数量。
- [ ] 6.3 消息跳转携带原 recordId，并在今日页校验与当前 activeRecord 匹配；验证旧任务结束后接取新任务，再点击旧提醒仍查看原记录，不误操作新任务。
- [ ] 6.4 完成／放弃后以及页面重新可见时更新任务与消息状态；验证跨设备完成、前台倒计时结束、后台过期后恢复页面均显示正确终态且不自动标记消息已读。

## 7. 联调与交付验证

- [ ] 7.1 在隔离 MySQL 联调提醒到期全流程，覆盖旧记录、新记录、多实例、双设备和截止前后竞争；记录三个 specs 中每个场景的测试或手工验收结果及未覆盖限制。
- [ ] 7.2 运行 lifeOnline 构建与相关 Jest／MySQL 集成测试、既有认证回归，以及 lifeOnline-h5 构建／类型检查与认证回归；保存实际结果，失败时修复本变更问题并注明既有失败，避免运行带自动修复参数的全仓检查污染无关修改。
- [ ] 7.3 验证先迁移、兼容后端、前端、启用期限的发布顺序及关闭新期限开关的回滚演练；交付明确操作说明，确认旧记录未补算、已产生期限仍执行、消息历史不丢失，不擅自操作生产环境。
