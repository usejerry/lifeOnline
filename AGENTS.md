# LifeOnline 项目协作规则

本文件适用于整个仓库。执行任务前先阅读现有代码和配置，并遵循项目已经采用的实现方式。

## 沟通与任务范围

- 默认使用中文与用户沟通，先说明结果，再补充必要的实现细节。
- 严格围绕用户当前请求工作，不擅自增加功能、重构无关模块或改变已有接口行为。
- 需求存在小范围歧义时，可结合现有代码作出保守假设；会明显影响功能或数据结构时，先向用户确认。
- 除非用户明确要求，否则不要使用 OpenSpec，不要创建或修改 OpenSpec 相关文件。
- 不覆盖、删除或格式化用户已有的未提交改动；发现工作区不干净时，只处理与当前任务相关的文件。

## 中文注释规范

- 新增或修改代码时，为关键业务逻辑添加简洁、准确的中文注释，方便初学者理解。
- 注释重点解释“为什么这样处理”、业务约束、数据流和容易踩坑的地方，不要逐行翻译代码。
- 数据库事务、并发控制、幂等处理、权限校验、时区转换、缓存策略和异常分支应添加中文注释。
- 复杂方法可在方法上方说明用途、主要步骤、输入输出和重要边界情况。
- 简单赋值、明显的方法调用、普通 CRUD 和代码已经清晰表达的内容不需要添加注释。
- 修改代码行为时同步更新相关注释，禁止保留与实现不一致的过期注释。

## TypeScript 与 NestJS

- 保持 TypeScript 类型明确，优先使用具体类型、接口或 DTO，避免新增 `any`、不必要的类型断言和非空断言。
- Controller 只负责路由、身份获取和参数接收；业务逻辑放在 Service；数据库结构放在 Entity。
- HTTP 输入必须使用 DTO 和 `class-validator` 校验，不直接信任请求体、查询参数或路径参数。
- 用户身份必须从 JWT 认证上下文获取，不能使用客户端提交的 `userId` 作为当前用户身份。
- 涉及用户数据的查询和更新必须包含用户范围限制，防止越权读取或修改其他用户的数据。
- 异步数据库操作必须正确 `await` 或返回 Promise，避免遗漏等待导致错误无法捕获。
- 优先复用项目已有模块、工具和编码风格；没有必要时不要新增生产依赖。

## 数据库与迁移

- 项目使用 MySQL 8 和 TypeORM；实体字段名、类型、长度、可空性、索引及外键必须与 SQL 迁移一致。
- 开发环境可以使用 `DB_SYNCHRONIZE=true`，正式环境必须 `DB_SYNCHRONIZE=false`。
- 新增或修改表结构时，必须在 `database/migrations/` 添加可用于正式环境的一次性 SQL 迁移。
- 迁移文件应说明执行条件和注意事项；不要在未获得用户明确授权时连接或修改正式数据库。
- 涉及时间时明确业务时区。数据库中的时间点默认按 UTC 存储；签到业务日期等本地日期按 `Asia/Shanghai` 计算。
- 并发和重复请求不能只依赖代码判断，应优先使用唯一索引、事务或其他数据库约束作为最终防线。

## 安全与错误处理

- 不在代码、日志、测试或文档中写入真实密码、Token、密钥和正式环境连接信息。
- 新增接口默认进行身份认证、输入校验和数据归属校验；确需公开访问时明确说明原因。
- 对外返回可理解且稳定的错误信息，不向客户端暴露堆栈、SQL、密钥或内部实现细节。
- 删除文件、数据或执行不可逆操作前，先确认目标和影响范围；没有明确授权时不执行破坏性操作。

## 修改与验证

- 修改前先使用 `rg` 等工具查找相关实现、调用方、测试和迁移，避免只改到局部。
- 保持改动小而集中，不顺手修复与当前任务无关的问题。
- 只格式化本次修改的文件，避免运行会重写整个仓库的自动修复命令。
- 修改 TypeScript 后至少运行与改动相关的测试和 `pnpm build`；必要时对改动文件运行 ESLint 和 Prettier 检查。
- 测试应覆盖正常路径以及权限、重复请求、空数据、异常和边界条件中与本次功能相关的部分。
- 如果因为现有环境、依赖或外部服务无法完成验证，应明确说明未验证的内容和原因，不把未运行的检查描述为已通过。

## 交付说明

- 完成后简要说明修改了什么、验证结果、尚未覆盖的范围以及正式部署需要执行的迁移或配置步骤。
- 引用仓库文件时给出清晰路径，方便初学者直接打开对应实现学习。
